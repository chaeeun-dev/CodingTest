// 두수의합 (TUKorea, 백준, 투포인터, 정렬)
// http://210.93.60.51/problem/0192

#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
	int n;
	cin >> n;

	vector<int> arr(n, 0);
	for (int i = 0; i < n; ++i)
	{
		cin >> arr[i];
	}

	int x;
	cin >> x;

	int count = 0;
	int end = 0;

	for (int start = 0; start < n; ++start)
	{

	}
}